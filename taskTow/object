
package main

import (
"fmt"
)

func main()  {
//定义一个 Shape 接口，包含 Area() 和 Perimeter() 两个方法。然后创建 Rectangle 和 Circle 结构体，实现 Shape 接口。在主函数中，创建这两个结构体的实例，并调用它们的 Area() 和 Perimeter() 方法
	var rectangle = Rectangle{area:12.5,leng:8}
	var circle = Circle{extent:29.6,length:12}
	gos(Shape(&rectangle))
	gos(Shape(&circle))

//使用组合的方式创建一个 Person 结构体，包含 Name 和 Age 字段，再创建一个 Employee 结构体，组合 Person 结构体并添加 EmployeeID 字段。为 Employee 结构体实现一个 PrintInfo() 方法，输出员工的信息
	var employee = Employee{Person:Person{Name:"zhangtao",Age:13}}
	(&employee).PrintInfo()

}

type Person struct{
	Name string
	Age int 
}

type Employee struct{
	Person
	EmployeeID int
}

func (em *Employee) PrintInfo(){
	fmt.Println("员工的名字:",em.Name)
	fmt.Println("员工的年龄:",em.Age)
}


type Shape interface{
	Area()
	Perimeter()
}

func gos(sh Shape){
	sh.Area()
	sh.Perimeter()
}

type Rectangle struct{
	area float64
	leng int
}

func (re *Rectangle) Area(){
	fmt.Println(re.area)
}

func (re *Rectangle) Perimeter() {
	fmt.Println(re.leng)
}

type Circle struct{
	extent float64
	length int
}

func (ci *Circle) Area(){
	fmt.Println(ci.extent)
}

func (ci *Circle) Perimeter() {
	fmt.Println(ci.length)
}
